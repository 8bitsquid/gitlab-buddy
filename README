
## Features

- [ ] Search for references to `master` branch
    - [ ] Check merge requests
    - [ ] Check code base
    - [ ] Check configs and modules
        - [ ] YAML
        - [ ] JSON
        - [ ] Terraform Modules ?
- [ ] Migrator
- [ ] Config command
    - [ ] Wizard
    - [ ] Temparary config vars (only used for current session)
- [ ] Groups
    - [ ] List
    - [ ] Settings
        - [ ] default branch
- [ ] Branches
    - [ ] List
- [ ] Projects
    - [ ] List
    - [ ] Settings
        - [ ] default branch


cmds
migrate branch <BRANC_NAME> <NEW_BRANCH> --repo=<REPO_ID> --client=<CLIENT_NAME> [[--set-default|--set-protected-default] [--archive|--archive-old-branch] [--keep-old-branch] [--omit-merge-requests]]
migrate branch BRANCH_NAME to NEW_BRANCH in REPO_ID
migrate branches BRANCH_NAME to NEW_BRANCH in group GROUP_ID --client=CLIENT_NAME

MigrateGroupSteps

1. Get all group projects
2. Create project in new group
3. clone 

MigrateBranchSteps

1. Check if new branch already exists
2. Check if repo has references to new branch
3. Check for submodules -> place into a queue to update when submodules are updated
2. Check if branch is protected
3. Move to new branch
4. archive to `archive` tag
5. ensure archive tag is protected
6. remove old branch